# Calorease — 项目归档

> 用于把整个项目迁移到新的对话 / 新的 Project。包含现状、所有踩过的坑与根因、以及新对话的启动提示词。
> 最后更新:2026-07-31。**这一版是原生重写(v9 世代)之后的文档**,替换掉之前那份「记账本 (Ledger)」的归档。

---

## 一、项目是什么

一个纯本地运行的个人卡路里记账 Android 应用。

| 项目 | 值 |
|---|---|
| 仓库 | `github.com/Vassucavo/Calorease`(公开) |
| 包名 | `app.calorease` |
| 应用名 | Calorease |
| 界面语言 | 中文 |
| 技术栈 | Kotlin + Jetpack Compose(原生,非 WebView) |
| 网络权限 | **无**(清单里零权限,连 INTERNET 都没有) |
| 数据存储 | `filesDir` 下的 JSON 文件,只在手机本地,不上传 |
| 编译方式 | GitHub Actions 云端编译,产物发到 Releases |
| 约束 | 用户只有手机,没有电脑,所有操作必须在 GitHub 网页完成 |

### 历史沿革(重要)

这个应用有两代:

- **v1–v8「记账本」** — 单文件 `index.html`,原生 HTML+CSS+JS,套在一个 WebView 壳里。
  仓库 `Vassucavo/Ledger`,包名 `app.ledger`,最后发布到 v13。**已停止维护**,
  但仓库还在,源码可以查。
- **v9 起「Calorease」** — Kotlin + Compose 原生重写。仓库 `Vassucavo/Calorease`,
  包名 `app.calorease`。重写发生在用户还没录入任何数据的时候,所以顺便把包名一起改干净了。

**包名从 `app.ledger` 改成 `app.calorease` 是一次性的、不可重来的操作。**
从现在起 `applicationId` 绝对不能再动 —— 改包名等于发布另一个应用,
装上去和旧版并存,数据一条都带不过来。

### 仓库结构

```
Calorease/
├── foods.json                    ← 食物库 208 条,故意放根目录:可以在 GitHub 网页上直接编辑
├── icon-192.png / icon-512.png
├── PROJECT.md                    ← 这份文档
├── settings.gradle.kts
├── build.gradle.kts
├── gradle.properties
├── gradle/libs.versions.toml     ← 依赖版本集中在这里
├── app/
│   ├── build.gradle.kts
│   └── src/
│       ├── main/
│       │   ├── AndroidManifest.xml      ← 零权限
│       │   ├── res/                     ← 图标、启动窗口底色、strings
│       │   └── java/app/calorease/
│       │       ├── MainActivity.kt      ← 唯一的 Activity
│       │       ├── data/                Models Store Repository Foods Backup
│       │       ├── logic/               Nutrition Calibration Validation Dates
│       │       └── ui/                  9 个界面文件 + theme/
│       └── test/java/app/calorease/     ← JVM 单元测试,55 项
└── .github/workflows/
    ├── build-apk.yml             ← push 到 main 时出包发 Release
    └── check.yml                 ← 每次 push 任意分支都编译 + 跑测试,不发布
```

### 分层

```
ui/        Compose 界面。只读 Repository 的 StateFlow,所有改动调 Repository 的方法
data/      Models(数据结构)、Store(落盘)、Repository(唯一状态源)、Foods、Backup
logic/     纯函数,不依赖安卓 API,所以能直接在 JVM 上测
```

`logic/` 和 `data/Store` 都不碰安卓 API,单元测试能覆盖到全部业务逻辑,不需要模拟器。

### 编译链

JDK 17 → `android-actions/setup-android@v3` → Gradle 8.10.2 → AGP 8.7.2 →
Kotlin 2.0.21 → Compose BOM 2024.10.01 → compileSdk/targetSdk 35,minSdk 24
(靠 core library desugaring 用 `java.time`)→ `gradle test` → `assembleRelease`
+ 固定密钥签名 → 检查 `assets/foods.json` 在不在包里 → `softprops/action-gh-release@v2`
发布 `calorease.apk`。

依赖只有 AndroidX 基础库、Compose、kotlinx-serialization。没有 Room、没有 Hilt、
没有任何网络库。

### 版本号

```
versionCode = github.run_number + codeOffset(=20)
versionName = "9.0.$versionCode"
release tag = v${{ github.run_number + 20 }}
```

三个数字必须对齐。`codeOffset` 定义在 `app/build.gradle.kts`,
**只能往上加,永远不要调小** —— 安卓不允许用更低的 versionCode 覆盖安装。
偏移 20 的由来见错误档案第 13 条。

### 签名密钥(关键)

存在 GitHub Secrets 里,四条:

| Name | 说明 |
|---|---|
| `KEYSTORE_BASE64` | `ledger.jks` 的 base64,约 3600 字符,无换行 |
| `KEYSTORE_PASSWORD` | (值只在 Secrets 和你自己的网盘备份里,不写进任何文档) |
| `KEY_ALIAS` | 同上 |
| `KEY_PASSWORD` | 同上 |

密钥文件名沿用 `ledger.jks`,有效期到 2053 年。
**这把密钥丢了,就再也无法覆盖更新这个应用**,只能卸载重装(数据全丢)。务必另存一份到网盘。

> 上一版归档把四个密码明文写在文档里。那份文档要和源码一起传进 Project 知识库,
> 而知识库里也放了 `.jks` 文件本身 —— 密钥和密码放在一起,等于没加密。
> 这一版只列名字不列值。**仓库根目录的 `.gitignore` 挡掉了 `*.jks`、`*.keystore`、
> `keystore-base64.txt`,不要绕过它。仓库是公开的。**

---

## 二、核心业务逻辑

这部分从 v8 到 v9 一个字都没改 —— 换的是实现语言和界面手感,不是业务规则。
单元测试是从 v8 的 jsdom 测试逐条搬过来的,测的是行为,所以能证明两版算出来的数一样。

### 消耗模型(`logic/Nutrition.kt`)

```
每日消耗 = 基础代谢(Mifflin-St Jeor) + 手表活动卡路里 + 手动运动条目
```

- 男:`10×kg + 6.25×cm − 5×age + 5`
- 女:`10×kg + 6.25×cm − 5×age − 161`

**刻意没有用活动系数(1.2/1.375/…)**,因为会和手表的活动卡路里重复计算。
代价是漏掉了食物热效应(约摄入的 10%),所以这个模型系统性偏低 —— 但校准会自动吸收这个偏差。

### 校准(`logic/Calibration.kt`,应用的核心)

```
真实每日消耗 = 区间内平均摄入 − (体重变化kg × 7700 ÷ 天数)
```

7700 = 每公斤体组织的大致热量。三道守卫,任一不满足就拒绝出数并说清楚差在哪:

1. 至少 2 条体重记录
2. 首尾跨度 ≥ 14 天(更短的区间里变化几乎都是水分)
3. 区间内**有摄入记录**的天数 ≥ 60%(摄入为 0 的那天不算)

设计哲学:**公式只是假设,身体的实际反应才是证据。** 这是整个应用存在的理由。
宁可不给数字,也不给一个基于两天水分波动算出来的假数字。

### 目标热量(两种模式)

- **按摄入**:固定每天吃多少。必须为正,且不低于下限(女 1200 / 男 1500)。
- **按结余**:填每天的缺口或盈余,**可以是负数**。减重填 −500,增重填 +300。
  可吃额度 = 当天消耗 + 结余目标,会随运动量浮动。绝对值上限 1500。

### 体重与体脂

- 体重记录带日期,可补录历史,可修改
- 只有**最新那条**才回写身体数据里的体重(它决定基础代谢)
- 体脂率可选(3–70 之间),用于算 **瘦体重 = 体重 × (1 − 体脂率)**
- 趋势线是 7 次记录的移动平均;界面反复强调看线不看点
- 趋势图横轴按**真实日期间隔**,不是按记录序号
- 会给出判断:脂肪降 + 瘦体重守住 = 理想;两个一起降 = 建议加蛋白质和力量训练

### 食物库(`foods.json` + `data/Foods.kt`)

208 条,13 个分类,两种计量方式:

- `"g"` = 每 100g 的数值(162 条)。选中后有 50/100/150/200/300 快捷按钮 + 自由输入
- `"x"` = 每份的数值(46 条,如小笼包、汉堡)。0.5/1/1.5/2 份数按钮

**`foods.json` 故意放在仓库根目录**,构建时由一个 Copy 任务搬进 assets。
这样加一条食物、改一个数值,在 GitHub 网页上编辑那个文件就行,不用碰 Kotlin。
`FoodsTest` 守着它:条目数、分类清单、计量方式、重名、每 100g 不超过 900 千卡
(纯脂肪的物理上限)、蛋白质换算成热量不超过总热量。

三种录入:搜索 / 快速录入(配合"拍照问 AI 再手填"的流程,有复制提示词按钮)/ 营养标签。
自己录入的会存进「我的食物」,搜索框空着时显示最近常吃 5 条。

### 数据与备份(`data/Store.kt`、`data/Backup.kt`)

- 文件名沿用 v8 的 localStorage 键名:
  `cl.profile.json` / `cl.mine.json` / `cl.history.json` / `cl.weights.json` / `cl.day.YYYY-MM-DD.json`
- 写入先写临时文件再改名,中途被杀进程不会留下半个 JSON
- 导出/恢复走安卓系统文件选择器(`CreateDocument` / `OpenDocument`),**不需要存储权限**
- 恢复支持「覆盖」和「合并」两种模式
- **备份格式和 v8 网页版完全一致**,字段名一个没改 —— 旧备份可以直接导进来。
  以后改数据结构时要考虑这一点,`BackupTest` 里有一条测试专门守着

---

## 三、错误档案

> 这一节是这份文档最有价值的部分。每条都记了**症状 → 根因 → 修法 → 可推广的教训**。
>
> 原生重写之后,其中 6 条在结构上不再成立(标注为「已消失」),
> 3 条被结构性挡住(标注为「已挡住」)。**「已消失」不等于教训作废** ——
> 那些教训是关于怎么诊断问题的,换个技术栈照样会以别的形式出现。

### 1. 全局变量遮蔽了浏览器内置对象 ★最隐蔽 · 已消失

- **症状**:系统返回键直接退到桌面;所有取消按钮点了没反应;添加餐食后不关闭面板。三个症状同时出现。
- **根因**:代码里写了 `let history = load(...)` 存历史记录,**遮蔽了 `window.history`**。于是 `history.pushState()` 变成在普通对象上调用不存在的方法,抛异常,被 `try/catch` 悄悄吞掉。
- **修法**:变量改名为 `logs`。
- **现状**:Kotlin 没有可以被污染的全局命名空间,这类问题不存在。
- **教训(仍然有效)**:**`try/catch` 里空吞异常会把根因掩埋掉。** 一个 catch 块什么都不做,就等于把未来某次排查的成本预支掉了。

### 2. CSS 优先级导致选中态失效 · 已消失

- **症状**:克数快捷按钮点击后颜色变了,但没有加深突出显示。
- **根因**:`body.glass .quick button` 权重 (0,2,2) 高于 `.quick button.on` 的 (0,2,1),主题样式把选中态覆盖掉了。
- **现状**:Compose 里选中与否是 `selected == value` 当场算出来的,没有样式层级可以盖掉它。
- **教训(仍然有效)**:引入"主题/皮肤"这类横切层时,必须检查它会不会静默吃掉状态表达。

### 3. backdrop-filter 在滚动容器里留残影 · 已消失

- **症状**:添加餐食的「搜索」页出现一条浅色无文字的空条,「快速录入」页没有。
- **根因**:安卓 WebView 里,给可滚动容器**内部**的元素加 `backdrop-filter`,合成层会留下渲染残影。
- **现状**:原生版不用模糊,浮层遮罩是一层半透明纯色。
- **教训(仍然有效)**:昂贵的视觉效果嵌套使用,既不省电也容易触发渲染 bug。先问它值不值。

### 4. 软键盘检测在 adjustResize 下永远失效 · 已消失

- **症状**:底部标签栏跟着键盘往上移,写好的隐藏逻辑从来没触发过。
- **根因**:`android:windowSoftInputMode="adjustResize"` 会让整个窗口缩小,于是 `window.innerHeight` 和 `visualViewport.height` **同步变小**,两者差值恒为 0。
- **修法**:记录见过的最大高度 `maxH`,用 `maxH − 当前高度 > 140` 判断。
- **现状**:`WindowInsets.ime` 就是系统给的真实键盘高度,不需要反推。
- **教训(仍然有效)**:**要测量一个量时,先确认参照物本身是不是也在变。** 拿两个同向变化的量作差,永远得 0。

### 5. safe-area 在 WebView 里产生空白条 · 已消失

- **症状**:面板底部一条浅色空白,会跟着键盘上移。
- **根因**:`viewport-fit=cover` + `env(safe-area-inset-bottom)`。
- **现状**:原生的 `WindowInsets` 是准的,可以放心用。
- **教训(仍然有效)**:同一个概念在两层里可能有两套实现,准确度不一样。别假设包装层的值等于底层的值。

### 6. CI 调试签名每次都不一样 ★后果最严重 · 仍然有效

- **症状**:新版 APK 装不上,或者提示必须先卸载 —— 而卸载会清空全部数据。
- **根因**:GitHub Actions 每次都是全新虚拟机,调试密钥是每台机器临时生成的。安卓不允许用不同签名覆盖安装。
- **修法**:生成一把固定密钥,base64 存进 GitHub Secrets,构建时解码并用 `assembleRelease` 签名。
- **教训**:**任何要长期迭代的自建应用,第一件事就是固定签名密钥。**
- **现状**:修法原样保留在新 workflow 里,并且未签名时会主动报错退出。

### 7. CI 里的相对路径错误(Gradle 显示成功但 job 失败) · 仍然有效

- **症状**:构建摘要里 `assembleRelease ✅`,但 job 报 `exit code 1`。
- **根因**:脚本先 `cd android` 再 `mkdir -p out`,目录建到了子目录里;而复制目标指向仓库根目录。
- **修法**:`mkdir -p "$GITHUB_WORKSPACE/out"` 放在 `cd` 之前;APK 用 `find` 搜索而不是写死文件名。
- **教训**:**先看失败发生在哪一步,再看是什么错。** 编译成功而 job 失败,说明问题在编译之后的环节。
- **现状**:两处写法都原样保留在新 workflow 里。

### 8. 异步查询 + 失败即退出 ★修了三次才对 · 已消失

- **症状**:系统返回键直接回桌面。改了两版都没好,而且症状完全一样。
- **根因**:`onBackPressed()` 用 `evaluateJavascript` **异步**问页面"要不要处理",而失败分支写成了 `finish()`。这条链路上有多个可能悄悄失败的环节,**任何一个出问题症状都一样**,无法区分。
- **修法**:反转数据流,页面主动调 `Android.setCanGoBack(bool)` 把状态推给原生层。
- **现状**:原生版用 `BackHandler`,同步,`enabled` 由当前 Compose 状态直接决定,没有跨层的桥。那套推状态的方案整个删掉了。
- **教训(仍然有效)**:
  - **同一个症状修两次没好,说明诊断错了,不是修得不够 —— 该换方法,并加诊断手段。**
  - 跨层通信优先用"推送状态"而不是"拉取查询",前者可测、可观察。
  - **失败的默认分支要选安全的那一侧。** "查不到就退出"是错的,应该是"查不到就什么都不做"。

### 9. 更新了值却忘了刷新 UI · 已挡住

- **症状**:克数快捷按钮的高亮永远停在初始的 100g。
- **根因**:`setAmount()` 更新了数值和预览,唯独漏了调用刷新按钮状态的 `drawQuickState()`。而另一条路径 `readAmount()` 调了。
- **现状**:状态只有一份(`Repository` 的 `StateFlow` / Compose 的 `remember`),UI 是它的函数。没有"第二处状态"可以忘记同步。
- **教训(仍然有效)**:同一状态有多条修改路径时,把"改状态 + 刷新"收成一个入口。更彻底的做法是让状态只有一份。

### 10. 校验之前就修改了数据 · 已挡住

- **症状**:修改体重记录时输入了不合法的值,报错后取消,原记录不见了。
- **根因**:`editWeight` 的回调先 `weights.filter()` 删掉旧记录,再调用带校验的保存函数。
- **修法**:改成所有校验通过后才动数据结构。
- **现状**:`logic/Validation.kt` 里全是纯函数,输入字符串,返回 `Checked.Valid` 或 `Checked.Invalid`。校验函数**拿不到可变状态**,想在校验前改数据都做不到。`Repository.saveWeight` 只接收已经校验过的 `WeightEntry`,自己不做校验。
- **教训(仍然有效)**:校验必须在任何副作用之前完成。**能用类型挡住的,就别靠纪律挡。**

### 11. 固定颜色匹配不了半透明面板 · 已消失

- **症状**:吸顶搜索框周围出现一条明显的颜色接缝。
- **根因**:渐变写的是固定色值,而玻璃面板是半透明叠在模糊背景上,**本质上无法用固定值精确匹配**。
- **修法**:平时完全透明,只有滚动到吸顶时才淡入渐变 —— 绕开匹配问题,而不是去解它。
- **教训(仍然有效)**:遇到"两个颜色对不上"的问题,先问一句能不能让其中一个消失。

### 12. 空记录残留污染统计 · 已挡住

- **症状**:某天的记录删空之后,历史里仍有一条全 0 的条目,拉低了校准的覆盖率。
- **修法**:检测到当天没有任何内容时,连同记录和历史条目一起清掉。
- **现状**:`Repository.commitDay()` 是当天记录的唯一写入口,清理逻辑在那一处。
- **教训(仍然有效)**:删除操作要清理所有派生数据,不只是主记录。

### 13. 换仓库导致 versionCode 降级 ★新增

- **症状**(预判到并提前避免的,没有真的踩上):新 APK 装不上,报 `INSTALL_FAILED_VERSION_DOWNGRADE`。
- **根因**:旧仓库 `Vassucavo/Ledger` 用 `github.run_number` 当 versionCode,最后发布到 v13。
  换到新仓库 `Vassucavo/Calorease` 之后 `run_number` **从 1 重新开始**,直接拿来用就是 1 < 13。
  症状和第 6 条一模一样(装不上、提示要先卸载),但根因完全不同。
- **修法**:加 `codeOffset = 20`,第一次构建出 versionCode 21。release tag 和 versionName 同步用这个数。
- **教训**:**从 CI 环境里取的"自增数字",作用域是那个仓库/那条流水线,不是你的应用。**
  换仓库、换 CI、重建流水线,这个数都会归零。任何被外部计数器喂养的单调量,
  都要问一句"它凭什么单调"。

### 14. 构建成功不等于产物正确 ★新增

- **症状**(同样是提前堵住的):应用能装能开,但搜索食物永远是空的。
- **根因**:`foods.json` 放在仓库根目录,靠一个 Gradle Copy 任务搬进 `assets/`。
  这条链断掉(改了 `sourceSets`、Copy 没在资源合并前跑、文件被 gitignore 挡掉)时,
  **构建照样全绿** —— 要装到手机上才发现,而且很难联想到是打包环节的问题。
- **修法**:CI 里 `unzip -l` 查一遍 APK,没有 `assets/foods.json` 就直接失败,发布路径上也查一遍。
- **教训**:**CI 绿了只证明命令退出码是 0,不证明产物是你要的东西。**
  凡是"构建期搬运"的产物,都要在构建后验证它真的到位了。这是第 7 条的反面:
  那次是编译成功但 job 失败,这次是 job 成功但产物不对。

---

## 四、流程上的教训

1. **发布前跑无头测试。** v8 时代是 Node + jsdom 模拟 localStorage/DOM/原生桥接;
   现在是 Gradle 里的 JVM 单元测试,55 项。`logic/` 和 `data/Store` 都不碰安卓 API,
   所以不需要模拟器。**测试写的是行为不是实现** —— 从 JS 搬到 Kotlin 时,
   断言几乎一条没改就能复用,这本身就是"测行为"的证据。
2. **出包之前先跑测试,红了就不出包。** 装上去才发现校准算错了,比构建失败难查得多。
3. **同一症状反复出现 = 诊断方向错了。** 返回键改了三版才对,前两版都是在错误的假设上打补丁。
4. **不确定问题出在哪一层时,先加诊断。** 返回键那次在设置页加了一行"原生桥接是否连接",
   立刻就能区分是页面问题还是 APK 没重新编译。
5. **失败的默认行为要保守。** 不确定时选"不退出""不删除""不导入",而不是相反。
   备份恢复认不出文件时什么都不做,就是这条。
6. **描述不清的 bug 就问,并配合截图。** 那条"空的透明条",靠三个问题
   (什么颜色、有无文字、何时出现)就排除了两个错误候选。

---

## 五、迁移到新对话 / 新 Project 的步骤

1. 把这份 `PROJECT.md` 传进 Project 知识库。
   源码不用传 —— 仓库是公开的,让新对话直接读 `github.com/Vassucavo/Calorease` 就行,
   而且这样永远是最新的。
2. **不要**把 `ledger.jks` 或任何密码传进知识库。密钥只放 GitHub Secrets + 你自己的网盘。
3. 新对话里粘贴下一节的启动提示词。

---

## 六、新对话启动提示词

> 复制下面整段,作为新对话里的第一条消息发送。

```
我们继续维护一个已经在运行的安卓应用 Calorease(卡路里记账)。
项目归档文档已经放在这个 Project 的知识库里,请先读一遍,
特别是「错误档案」那一节 —— 那里记录了 14 个已经踩过的坑和根因,不要再犯。

## 快速背景

- 个人卡路里记账应用,纯本地,零权限,数据只存在手机上
- Kotlin + Jetpack Compose 原生应用(v9 起。v1–v8 是 WebView 套单文件 HTML,已废弃)
- 仓库 Vassucavo/Calorease,公开,Android 工程直接在仓库里
- 我只有手机,没有电脑,所有操作必须能在 GitHub 网页上完成 ——
  所以代码由你改、推分支、开 PR,我只负责点合并、装包、验收
- 签名密钥已固定并存在 GitHub Secrets,更新可以覆盖安装、数据保留

## 硬性约束(改动时必须遵守)

1. applicationId 和 namespace 永远是 app.calorease,绝对不能改
2. app/build.gradle.kts 里的 codeOffset 只能往上加,永远不要调小
3. 只用本地存储,不引入任何联网、后台服务或需要 API key 的功能;
   AndroidManifest 保持零权限
4. 依赖尽量少。现在只有 AndroidX 基础库、Compose、kotlinx-serialization,
   要加新依赖先跟我说明为什么非加不可
5. 任何校验都必须在修改数据之前完成 —— 校验写成纯函数,返回 Checked.Valid/Invalid,
   让"校验前改数据"在类型上做不到
6. 颜色一律从 LocalColors 取,不要硬编码色值;新增界面要同时顾到浅色和深色
7. 食物库改动只动根目录的 foods.json,不要把数据塞进 Kotlin 代码
8. 备份格式向后兼容 —— 改数据结构时要保证旧备份还能导入,并补一条测试
9. 失败的默认分支要选安全的一侧(不退出、不删除、不导入)
10. 界面文字用中文,单位(kg/cm/g)和 Mifflin-St Jeor 这类专有名词保留原文

## 我期望的工作方式

- 改完之后跑单元测试验证关键路径,推到分支等 CI 绿了再告诉我
- CI 有两个 workflow:check.yml 每次推分支都编译+测试(不发布),
  build-apk.yml 只在 main 上跑并发 Release
- 给我开 PR,我在网页上点合并;不要直接推 main
- 有不确定的地方直接问我,可以用截图配合排查,不要猜

## 这次我想做的改动

[在这里写你的需求]
```

---

## 七、还没做的 / 可以考虑的

- **真机验收还没做完** — 原生版 v21 是第一个包,界面手感、键盘遮挡、
  趋势图渲染、深色模式配色都还没在真机上确认过
- **食物数值精度**:208 条都是常见参考值,中式菜肴(尤其带油的)误差可能不小
- **食物库扩充**:目前没有按品牌/连锁餐厅的条目
- **日期选择器**:体重补录目前是「今天/昨天/前天」快捷键 + 手填 YYYY-MM-DD,
  更早的日期填起来略麻烦,可以换成原生 DatePicker
- **多设备同步**:当前完全本地,换手机只能靠导出/恢复
- **提醒**:没有记账提醒功能
- **数据可视化**:只有体重趋势折线,摄入/消耗没有图表
- **界面动效**:目前只用了 Compose 的默认动画,页面切换还没做转场

---

## 八、关于数字的一贯说明

食物表、Mifflin-St Jeor 公式、7700 这个常数、手表的活动卡路里 —— 全都是估算值。
这个应用的价值不在于任何单个数字有多准,而在于连续记录几周后,校准能把系统性误差抵消掉。
看趋势,不要看单日。

要针对个人健康状况设定目标,应当咨询医生或注册营养师。
