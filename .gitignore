# 签名私钥绝不进仓库(仓库是公开的)。
# 密钥只放在 GitHub Secrets 的 KEYSTORE_BASE64 里,外加一份网盘离线备份。
*.jks
*.keystore
keystore-base64.txt
key.properties

# 构建产物
out/

# Gradle / Android 构建产物
build/
.gradle/
local.properties
*.iml
.idea/
.cxx/
