# 签名私钥绝不进仓库(仓库是公开的)。
# 密钥只放在 GitHub Secrets 的 KEYSTORE_BASE64 里,外加一份网盘离线备份。
*.jks
*.keystore
keystore-base64.txt
key.properties

# 网页壳版本的 CI 会在这里临时生成整个 Android 工程,不入库。
# 原生工程在仓库根目录的 app/,和这个 android/ 没关系。
android/
out/

# Gradle / Android 构建产物
build/
.gradle/
local.properties
*.iml
.idea/
.cxx/
